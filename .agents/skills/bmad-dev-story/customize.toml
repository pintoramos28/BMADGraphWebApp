# DO NOT EDIT -- overwritten on every update.
#
# Workflow customization surface for bmad-dev-story. Mirrors the
# agent customization shape under the [workflow] namespace.

[workflow]

# --- Configurable below. Overrides merge per BMad structural rules: ---
#   scalars: override wins • arrays (persistent_facts, activation_steps_*): append
#   arrays-of-tables with `code`/`id`: replace matching items, append new ones.

# Steps to run before the standard activation (config load, greet).
# Overrides append. Use for pre-flight loads, compliance checks, etc.

activation_steps_prepend = []

# Steps to run after greet but before the workflow begins.
# Overrides append. Use for context-heavy setup that should happen
# once the user has been acknowledged.

activation_steps_append = []

# Persistent facts the workflow keeps in mind for the whole run
# (standards, compliance constraints, stylistic guardrails).
# Distinct from the runtime memory sidecar — these are static context
# loaded on activation. Overrides append.
#
# Each entry is either:
#   - a literal sentence, e.g. "All stories must include testable acceptance criteria."
#   - a file reference prefixed with `file:`, e.g. "file:{project-root}/docs/standards.md"
#     (glob patterns are supported; the file's contents are loaded and treated as facts).

persistent_facts = [
  "file:{project-root}/**/project-context.md",
]

# Scalar: executed when the workflow reaches its final step,
# after the story implementation is complete and status is updated. Override wins.
# Leave empty for no custom post-completion behavior.

on_complete = ""

# Story-loop orchestration mode. Defaults keep normal dev-story behavior.
# Team/user overrides can enable this when launched by bmad-story-loop-orchestrator.

[workflow.story_loop]

# Scalar: when true, the workflow follows the orchestrated worker contract.
# A launch prompt from bmad-story-loop-orchestrator should also activate this
# behavior even if the resolved value remains false.

enabled = false

# Scalar: implementation workers may use helper subagents only when those
# subagents are explicitly read-only and cannot create conflicting writes.

allow_readonly_helper_subagents = true

# Scalar: optional JSON schema path for terminal report validation.

terminal_report_schema = ""

# Scalar: accepted values are "blocked" or "best-effort" when status sync is
# unclear. Story-loop mode should use "blocked" so the orchestrator can decide.

ambiguous_status_sync = "best-effort"
