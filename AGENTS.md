# Repository Agent Instructions

When spawning sub-agents with the task/delegation tool, use `subagent_type: "general-purpose"` by default unless the user explicitly requests a different sub-agent type or a more specific specialized agent is required.

This repo also sets OpenCode's default primary agent to `general-purpose` in `.opencode/opencode.json`.
