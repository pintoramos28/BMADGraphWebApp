::code-comment{title="[P2] AC4 fallback coverage still misses direct `/unsupported` delivery" body="Story 1.7 AC4 requires the local and hosted delivery paths to prove SPA fallback for `/`, `/workspace`, `/workspace/:workspaceId`, `/review/:workspaceId`, and `/unsupported`. The acceptance suite exercises `/`, `/workspace`, and the protected detail routes, but it never directly requests `/unsupported` through either `preview:shell` or the documented `npm run dev` flow. The only remaining evidence for `/unsupported` is the unit-level `isShellRoutePathname('/unsupported')` predicate, which would not catch a server-side regression where that path 404s instead of serving `index.html`. That leaves the route-inventory acceptance only partially evidenced." file="/home/pinto/repo/BMADGraphWebApp/tests/e2e/shell-delivery.spec.ts" start=181 end=286 priority=2 confidence=0.92}

**Findings**
1. `P2` Acceptance evidence is still incomplete for AC4. The documented local flow is now covered, and the hosted preview path covers most of the route inventory, but neither delivery-mode smoke suite directly validates `/unsupported` as a server-served SPA fallback route. Because AC4 names that route explicitly, the current evidence stops at a helper-level route predicate instead of proving the actual delivery behavior.

No other substantive acceptance, scope-drift, or workflow/status issues were evident from the scoped materials. Story and sprint status are consistent at `review`.
