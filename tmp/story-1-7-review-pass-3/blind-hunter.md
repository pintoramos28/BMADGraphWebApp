::code-comment{title="[P1] Preview server still depends on src TypeScript modules at runtime" body="`scripts/shell-bootstrap-metadata.mjs` validates the built JSON by importing `../src/schemas/api/release-manifest.ts` and `../src/schemas/api/support-matrix.ts`. That means the standalone delivery path is not actually self-contained: any deployment that ships `dist/` plus the thin server, but not the repo's `src/` tree, will fail before serving traffic. The earlier fix removed runtime reads from fixtures, but this keeps a different source-tree dependency in the hosted path." file="/home/pinto/repo/BMADGraphWebApp/scripts/shell-bootstrap-metadata.mjs" start=1 end=4 priority=1 confidence=0.92}

::code-comment{title="[P2] 500 responses leak internal server details to clients" body="The top-level error handler sends `error.message` directly in the HTTP response body for all non-`URIError` failures. If the generated `dist/api/*.json` files are missing, malformed, or schema-invalid, callers will receive raw filesystem and validation details instead of a generic failure response. For a hosted shell endpoint, those details should stay in server logs." file="/home/pinto/repo/BMADGraphWebApp/scripts/shell-delivery-server.mjs" start=203 end=210 priority=2 confidence=0.86}

Those are the substantive issues I found in the scoped diff.
