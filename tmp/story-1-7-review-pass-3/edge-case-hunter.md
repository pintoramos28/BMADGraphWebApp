::code-comment{title="[P2] Dev server does not fail closed for unknown /api routes" body="The Vite middleware only handles exact GET matches for the three bootstrap endpoints and calls `next()` for everything else under `/api`. In practice that lets browser-style requests such as `GET /api/not-real` with `Accept: text/html` fall through to Vite's history fallback and return `index.html` with 200, while the standalone hosted server correctly returns 404 JSON. That makes local validation less strict than the deployed path and weakens the 'narrow operational surface' contract; the current dev-flow E2E only exercises happy-path GETs, so this drift is not covered." file="/home/pinto/repo/BMADGraphWebApp/vite.config.ts" start=47 end=62 priority=2 confidence=0.98}
::code-comment{title="[P2] /api/health is not probe-safe across environments" body="The hosted delivery server rejects every non-GET request before it reaches `/api/health`, so a standard `HEAD /api/health` probe gets `405 Method Not Allowed` even when the service is healthy. In dev, the same request skips the middleware and is answered by Vite's SPA fallback as `200 text/html`, which is a false positive. That combination creates an environment-specific readiness gap and makes local checks misleading, and there is no E2E coverage for HEAD/OPTIONS behavior." file="/home/pinto/repo/BMADGraphWebApp/scripts/shell-delivery-server.mjs" start=149 end=181 priority=2 confidence=0.93}

Findings:
- `P2`: local `vite` delivery does not reserve the `/api/*` namespace fail-closed. Unknown browser-style `/api/*` requests can return the app shell instead of a miss, unlike `preview:shell`.
- `P2`: `/api/health` is inconsistent for probe methods. `preview:shell` returns `405` for `HEAD`, while local dev returns `200 text/html` via SPA fallback.

I did not find additional substantive edge-case issues inside the scoped changes beyond those two.
