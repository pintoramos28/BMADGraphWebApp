::code-comment{title="[P1] Budget-exceeded state starts after file reading, not when import starts" body="AC3 requires a visible in-progress state whenever preview readiness exceeds the 5s NFR1 budget. Here the budget timer and parsing status are only started inside `beginWorkerImport()`, but `handleFileImport()` awaits `file.arrayBuffer()` or `file.text()` first. For a large local file, that pre-worker read can consume the entire budget while the UI still shows the idle state, so the required in-progress feedback is never surfaced for that portion of the import." file="/home/pin81845/repo/BMADGraphWebApp/src/features/import/workspace-import-route.tsx" start=134 end=179 priority=1 confidence=0.96}
::code-comment{title="[P2] CSV/paste parsing does not use Papa Parse preview mode" body="The story checklist explicitly marks CSV and pasted-table parsing as using Papa Parse preview support, but this implementation parses the full text payload every time. `Papa.parse()` is called without a `preview` limit, so the code does not match the checked-off task and it weakens the AC3/NFR1 performance story for larger inputs." file="/home/pin81845/repo/BMADGraphWebApp/src/features/import/parse-import-preview.ts" start=32 end=36 priority=2 confidence=0.93}

- `src/features/import/workspace-import-route.tsx:134-179` — AC3 / NFR1 gap: the visible in-progress budget handling begins only after `file.text()` / `file.arrayBuffer()` completes, so imports that spend more than 5 seconds reading the local file can exceed the budget without ever showing the required in-progress state.
- `src/features/import/parse-import-preview.ts:32-36` — Task checklist mismatch: the story marks “Parse CSV and pasted tables with Papa Parse preview support” as done, but the implementation parses the entire payload with no `preview` option.

I found no other story-scoped acceptance gaps beyond those two.
