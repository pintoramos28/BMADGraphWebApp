::code-comment{title="[P2] Missing asset requests are rewritten to index.html" body="The new static delivery server falls back to `index.html` for every non-API miss, not just the documented shell routes. That means a missing JS/CSS chunk under `/assets/...` will return `200 text/html` instead of `404`, which turns stale-cache or partial-deploy problems into opaque bootstrap failures when the browser tries to execute HTML as a script. The fallback needs to be limited to route-shaped paths (`/`, `/workspace`, `/workspace/:id`, `/review/:id`, `/unsupported`) and leave unknown asset paths as real 404s." file="/home/pinto/repo/BMADGraphWebApp/scripts/shell-delivery-server.mjs" start=214 end=221}
::code-comment{title="[P3] Dev delivery no longer enforces the same metadata invariants as preview:shell" body="`preview:shell` rejects inconsistent bootstrap metadata up front by checking the manifest's pinned support-matrix version and URL, but the Vite dev middleware serves whatever JSON is on disk and still reports `/api/health` as OK. That creates an environment-specific gap where `npm run dev` can appear healthy while the hosted delivery path fails hard on the same metadata set. There is also no test asserting parity between the Vite middleware and the standalone delivery server for these failure cases." file="/home/pinto/repo/BMADGraphWebApp/vite.config.ts" start=25 end=36}

Findings:
- P2: [scripts/shell-delivery-server.mjs](/home/pinto/repo/BMADGraphWebApp/scripts/shell-delivery-server.mjs:214) rewrites all non-API misses to `index.html`, including missing hashed assets. In a static deployment, that masks broken bundle delivery and produces harder-to-diagnose shell startup failures instead of a clear 404.
- P3: [vite.config.ts](/home/pinto/repo/BMADGraphWebApp/vite.config.ts:25) does not enforce the same bootstrap metadata consistency checks as [scripts/shell-delivery-server.mjs](/home/pinto/repo/BMADGraphWebApp/scripts/shell-delivery-server.mjs:80). Local dev and hosted preview can therefore diverge on the same fixture set, and the new tests only exercise the preview server path.

No other substantive edge-case issues stood out within the scoped files.
