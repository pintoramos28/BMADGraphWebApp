1 finding.

- `P2` [src/services/release/shell-bootstrap-metadata.ts](/home/pinto/repo/BMADGraphWebApp/src/services/release/shell-bootstrap-metadata.ts:27): the new bootstrap validation only checks schema shape, manifest/support version alignment, and the derived `/api/health` digests. It never verifies that `releaseManifest.integrity.manifestSha256` matches the emitted manifest bytes, even though the contract still carries that field and ADR-09 says manifest integrity must be verifiable ([IK-ADR-09-release-manifest-and-compatibility-envelope.md](/home/pinto/repo/BMADGraphWebApp/_bmad-output/planning-artifacts/implementation-adrs/IK-ADR-09-release-manifest-and-compatibility-envelope.md:67)). A stale or copy-pasted `manifestSha256` therefore still passes `validateShellBootstrapMetadata`, still yields `status: "ok"` from `/api/health`, and still passes the added contract coverage because the tests only assert token format/alignment ([src/schemas/contracts.spec.ts](/home/pinto/repo/BMADGraphWebApp/src/schemas/contracts.spec.ts:129)). That leaves a real integrity/drift blind spot in the hosted-shell bootstrap path.

No other substantive edge-case or route/fallback issues stood out in the scoped changes.
