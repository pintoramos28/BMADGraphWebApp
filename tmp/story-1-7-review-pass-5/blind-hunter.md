::code-comment{title="[P1] Preview metadata validation regressed from full schema parsing to partial field checks" body="The new standalone preview helper no longer enforces the full release-manifest/support-matrix contracts before serving `/api/release-manifest`, `/api/support-matrix`, or reporting `/api/health` as `ok`. In `src/services/release/shell-bootstrap-metadata.ts`, the TypeScript helper parses both payloads through the Zod schemas, but this new `.mjs` version only checks a small subset of fields. That means schema-invalid deployed assets such as a bad `releaseDate`, invalid `telemetry.schemaVersion`, malformed `integrity.manifestSha256`, or malformed browser entries can now pass preview validation and be served as healthy. This is a real fail-closed regression for the static delivery path, and the added tests do not cover those invalid-but-still-accepted cases." file="/home/pinto/repo/BMADGraphWebApp/scripts/shell-bootstrap-metadata.mjs" start=28 end=85 priority=1 confidence=0.96}

1 substantive finding. No additional diff-visible issues rose above review noise.
